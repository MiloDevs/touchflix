/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.themoviedb.org",
      "assets.nflxext.com",
      "s6.imgcdn.dev",
    ],
  },
};

module.exports = nextConfig
